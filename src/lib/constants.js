export const ServerAddress = new URL(document.URL)


//To debug it change it
// in case of any errors please verify that it's not caused by having this file in .gitignore!!!!
export let MutServerAddress = new URL(document.URL)